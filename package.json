{
  "name": "vercel-localhost-router",
  "version": "0.1.0",
  "private": true,
  "description": "A tiny Vercel static iframe router that renders localhost:5173.",
  "scripts": {
    "dev": "vercel dev"
  },
  "engines": {
    "node": "22.x"
  }
}
