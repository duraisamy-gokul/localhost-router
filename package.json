{
  "name": "vercel-localhost-router",
  "version": "0.1.0",
  "private": true,
  "description": "A tiny Vercel static redirector that sends browser requests to localhost:5173.",
  "scripts": {
    "dev": "vercel dev"
  },
  "engines": {
    "node": "22.x"
  }
}
